import AdminWrapper from "@/components/admin/AdminWrapper";

export const metadata = {
  title: "Admin Panel | Antalya VIP Transfer",
  robots: "noindex, nofollow",
};

export default function AdminPage() {
  return <AdminWrapper />;
}
